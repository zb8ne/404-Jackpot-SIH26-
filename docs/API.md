# Credential Registry API

This document is the frontend/backend contract for the implemented API, including Phase 2 audit/monitoring and Phase 3 citizen-consent verification. The default local base URL is `http://localhost:8088`.

## Authentication, roles, and errors

Protected requests require `Authorization: Bearer <Supabase access token>`. The backend validates the token, loads the backend-owned SQLite profile by Supabase `sub`, and derives role and department from that profile.

Government and citizen records remain separate. `GET /account` is the unified session bootstrap endpoint; government-only operations continue to load `user_profiles` through RBAC middleware.

- `CONTROLLER`: system-wide audit and monitoring only.
- `ADMIN`: credential operations and audit history for its own department.
- `OFFICIAL`: issue, verify, and department credential reads; no revoke, supersede, audit, or monitoring access.

Application-handler errors are JSON:

```json
{"error":"message"}
```

Authentication and profile-loading middleware currently return `text/plain` for `401` and some `403`/`500` failures. Frontends must accept either representation. Common failures are missing/invalid bearer token (`401`), inactive/missing profile or forbidden role/scope (`403`), malformed input (`400`), missing credential/document (`404`), already-stamped upload (`409`), local failure (`500`), and blockchain read failure (`502`).

## Public endpoints

### GET `/health`

Purpose: backend and configured-contract health. Authentication/role/scope: none.

Request: no parameters or body.

Response `200`:

```json
{"ok":true,"contract":"0x1234..."}
```

Errors: none generated by the handler.

Example:

```sh
curl http://localhost:8088/health
```

### GET `/departments`

Purpose: list active application departments whose document type matches chain signer configuration. Authentication/role/scope: none.

Request: no parameters or body.

Response `200`:

```json
[
  {
    "id":"birth",
    "slug":"birth",
    "name":"Birth Registration Dept",
    "docType":1,
    "docTypeName":"birth_certificate",
    "address":"0x1234..."
  }
]
```

Errors: `500 {"error":"<database error>"}`.

Example:

```sh
curl http://localhost:8088/departments
```

## Identity

### GET `/account`

Purpose: resolve an authenticated Supabase identity to exactly one backend-owned government or citizen account. Authentication: required. Scope: the caller only.

Government response `200`:

```json
{
  "accountType":"GOVERNMENT",
  "id":"supabase-user-id",
  "email":"official@example.gov",
  "governmentProfile":{
    "id":"supabase-user-id",
    "email":"official@example.gov",
    "name":"Birth Official",
    "role":"OFFICIAL",
    "active":true,
    "department":{"id":"birth","name":"Birth Registration Dept","docType":1,"docTypeName":"birth_certificate"}
  },
  "citizenProfile":null
}
```

Citizen response `200`:

```json
{
  "accountType":"CITIZEN",
  "id":"citizen-asha",
  "email":"asha@example.test",
  "governmentProfile":null,
  "citizenProfile":{"id":"citizen-asha","email":"asha@example.test","displayName":"Asha Menon","active":true}
}
```

Errors: `401` authentication failure; `403` no linked account or inactive account; `409` the same Supabase identity is linked to both account tables; `500` lookup failure.

The selected login card is presentation state only. The frontend compares it with this backend response and signs out on a mismatch; it never grants a role.

### GET `/me`

Purpose: return the backend-owned application profile. Authentication: required. Roles: any active Controller, Admin, or Official. Scope: the caller only.

Response `200` for a department user:

```json
{
  "id":"supabase-user-id",
  "email":"official@example.gov",
  "name":"Birth Official",
  "role":"OFFICIAL",
  "active":true,
  "department":{"id":"birth","name":"Birth Registration Dept","docType":1,"docTypeName":"birth_certificate"}
}
```

A Controller has `"department":null`.

Errors: `401` authentication failure; `403` missing/inactive application profile; `500` profile-loading failure.

Example:

```sh
curl -H 'Authorization: Bearer <token>' http://localhost:8088/me
```

## Department credential reads

### GET `/citizen/credentials`

Purpose: list every stamped credential issued to the authenticated citizen account, enriched with current on-chain status. Authentication: required. Account type: active Citizen. Ownership is derived from the verified Supabase `sub`; the request accepts no citizen identifier.

Response `200` uses the same credential objects as `GET /credentials/{citizen}`. Errors: `401`; `403` missing/inactive citizen account; `409` ambiguous dual-linked identity; `500` storage failure.

### GET `/citizen/documents/{hash}/download`

Purpose: download the stored stamped PDF only when its `citizen_account_id` equals the authenticated citizen account. Authentication: required. Account type: active Citizen.

Errors: `401`; `403` document belongs to another citizen or the identity is not an active Citizen; `404` unknown document; `409` ambiguous dual-linked identity; `500` storage failure.

### GET `/citizens`

Purpose: list citizens having a stored document of the caller's department document type. Authentication: required. Roles: Admin or Official. Scope: caller department.

Response `200`:

```json
["Asha Menon","Rahul Iyer"]
```

Errors: `401`, `403`, or `500 {"error":"<database error>"}`.

Example:

```sh
curl -H 'Authorization: Bearer <token>' http://localhost:8088/citizens
```

### GET `/credentials/{citizen}`

Purpose: list stored credential history for a citizen, enriched with live on-chain status. Authentication: required. Roles: Admin or Official. Scope: caller department.

Path parameter: `citizen`, URL encoded.

Response `200`:

```json
{
  "citizen":"Asha Menon",
  "documents":[{
    "docHash":"0xabc...",
    "docId":"BC-2019-004471",
    "docType":"birth_certificate",
    "citizen":"Asha Menon",
    "issuer":"0x1234...",
    "filename":"certificate.pdf",
    "txHash":"0xtx...",
    "issuedAt":"2026-08-18 10:30:45",
    "sizeBytes":12345,
    "citizenAccountId":"citizen-asha",
    "status":"VALID"
  }]
}
```

`documents` is an empty array when no scoped documents exist. Status is `VALID`, `SUPERSEDED`, `REVOKED`, or `UNKNOWN`.

Errors: `401`, `403`, or `500` database failure.

Example:

```sh
curl -H 'Authorization: Bearer <token>' 'http://localhost:8088/credentials/Asha%20Menon'
```

### GET `/documents/{hash}/download`

Purpose: download the exact stamped PDF stored for an anchored hash. Authentication: required. Roles: Admin or Official. Scope: caller department.

Path parameter: exact `hash` stored by the API.

Response `200`: raw PDF bytes with `Content-Type: application/pdf`.

Errors: `401`; `403 {"error":"document is outside the authenticated department"}`; `404 {"error":"no such document"}`; `500` store failure.

Example:

```sh
curl -H 'Authorization: Bearer <token>' -o credential.pdf \
  http://localhost:8088/documents/0xabc.../download
```

## Credential operations

All four operation families create Phase 2 audit records. A valid negative verification verdict is an audit `SUCCESS`; it is not an operational failure.

### POST `/issue`

Purpose: stamp, hash, anchor, and store a new credential. Authentication: required. Roles: Admin or Official. Scope: caller department.

Multipart form-data:

- `file`: required non-empty PDF, maximum exactly 20 MiB. A larger file is rejected; it is never truncated or processed partially.
- `dept`: compatibility field; if supplied, must equal the caller's department.
- `doc_type`: required and must equal the department document type.
- `doc_id`: required non-empty document ID.
- `citizen`: legacy compatibility field. The backend uses the selected citizen account's stored display name.
- `citizen_account_id`: required stable ID of an active provisioned citizen account. The backend derives the stored citizen display name and email linkage from this record; it does not authorize ownership from the submitted `citizen` field.

Response `201`:

```json
{
  "docHash":"0xabc...",
  "txHash":"0xtx...",
  "issuer":"Birth Registration Dept",
  "docType":"birth_certificate",
  "docId":"BC-2026-1234",
  "citizen":"Asha Menon",
  "citizenAccountId":"citizen-asha",
  "downloadUrl":"/documents/0xabc.../download",
  "stamped":true
}
```

Errors: `400` malformed upload, file larger than 20 MiB, unknown document type, missing fields, or invalid/inactive citizen account; `401`; `403` role/department/contract rejection; `409` upload already contains a registry marker; `500` off-chain save/configuration failure.

Example:

```sh
curl -H 'Authorization: Bearer <token>' \
  -F file=@certificate.pdf -F dept=birth -F doc_type=birth_certificate \
  -F doc_id=BC-2026-1234 -F 'citizen=Asha Menon' -F citizen_account_id=citizen-asha \
  http://localhost:8088/issue
```

### POST `/verify`

Purpose: verify uploaded bytes using hash-first resolution. Authentication: required. Roles: Admin or Official. Scope: caller department.

Multipart form-data: required non-empty `file`, maximum exactly 20 MiB. A larger file is rejected and is not verified using truncated bytes.

Response `200`:

```json
{
  "filename":"certificate.pdf",
  "computedHash":"0xabc...",
  "expectedHash":"0xabc...",
  "docId":"BC-2026-1234",
  "status":"VALID",
  "message":"authentic and current",
  "docType":"birth_certificate",
  "issuer":"0x1234...",
  "timestamp":1787049045,
  "prevHash":"0x0000...",
  "citizen":"Asha Menon",
  "originalFilename":"certificate.pdf"
}
```

`status` is `VALID`, `SUPERSEDED`, `REVOKED`, `TAMPERED`, `NOT_ISSUED`, or `UNKNOWN`. Unknown documents omit unavailable `docType`, `issuer`, `timestamp`, `prevHash`, `citizen`, and `originalFilename`. Superseded results may add `"supersededBy":{"docId":"...","hash":"0x..."}`. ID fallback may add `"resolvedDocId":"..."`.

Errors: `400` malformed upload or file larger than 20 MiB; `401`; `403` role or cross-department result; `500` department configuration failure; `502` blockchain read failure.

Example:

```sh
curl -H 'Authorization: Bearer <token>' -F file=@certificate.pdf http://localhost:8088/verify
```

### GET `/verify/{docId}`

Purpose: legacy direct lookup by document ID. Authentication: required. Roles: Admin or Official. Scope: caller department. For a credential linked to a citizen account, this endpoint returns `409` and requires the Phase 3 verification-request flow. Old unlinked credentials retain the Phase 1/2 response for compatibility. The QR frontend never calls this endpoint.

Path parameter: URL-encoded `docId`.

Response `200` uses the `/verify` verdict fields but omits `filename` and `computedHash`:

```json
{
  "docId":"BC-2026-1234",
  "expectedHash":"0xabc...",
  "status":"VALID",
  "message":"authentic and current",
  "docType":"birth_certificate",
  "issuer":"0x1234...",
  "timestamp":1787049045,
  "prevHash":"0x0000...",
  "citizen":"Asha Menon",
  "originalFilename":"certificate.pdf"
}
```

Errors: `401`; `403` role or cross-department result; `409 {"error":"citizen consent is required; create a verification request"}` for linked credentials; `500` configuration failure; `502` blockchain failure.

Example:

```sh
curl -H 'Authorization: Bearer <token>' http://localhost:8088/verify/BC-2026-1234
```

### POST `/revoke`

Purpose: revoke a credential on-chain. Authentication: required. Role: Admin. Scope: caller department and original department signer.

JSON body:

```json
{"docHash":"0xabc...","dept":"birth"}
```

`dept` may be empty for compatibility; if present, it must equal the profile department.

Response `200`:

```json
{"docHash":"0xabc...","txHash":"0xtx...","status":"REVOKED"}
```

Errors: `400` malformed JSON/hash; `401`; `403` role, department, or contract rejection; `404 {"error":"no such credential"}`; `500` configuration failure; `502` blockchain read failure.

Example:

```sh
curl -H 'Authorization: Bearer <token>' -H 'Content-Type: application/json' \
  -d '{"docHash":"0xabc...","dept":"birth"}' http://localhost:8088/revoke
```

### POST `/supersede`

Purpose: anchor a replacement while preserving and superseding the old record. Authentication: required. Role: Admin. Scope: caller department and original department signer.

Multipart form-data:

- `file`: required replacement PDF, maximum exactly 20 MiB. A larger file is rejected; it is never truncated or processed partially.
- `dept`: compatibility field; if supplied, must match caller department.
- `old_hash`: required 32-byte hexadecimal credential hash.
- `doc_id`: required replacement document ID.
- `citizen_account_id`: optional for a linked old credential because its citizen linkage is inherited. Required for a legacy unlinked old credential and must identify an active provisioned citizen account.
- `citizen`: compatibility display field. The backend ignores it when linkage can be inherited or derived and stores the selected account's display name.

Response `201`:

```json
{
  "docHash":"0xnew...",
  "oldHash":"0xold...",
  "txHash":"0xtx...",
  "docId":"BC-2026-1234-R1",
  "citizen":"Asha Menon",
  "downloadUrl":"/documents/0xnew.../download",
  "stamped":true
}
```

Errors: `400` malformed upload/hash, file larger than 20 MiB, or missing fields; `401`; `403` role, department, or contract rejection; `404` old credential not found; `500` off-chain save/configuration failure; `502` blockchain read failure.

Example:

```sh
curl -H 'Authorization: Bearer <token>' -F file=@replacement.pdf -F dept=birth \
  -F old_hash=0xold... -F doc_id=BC-2026-1234-R1 -F citizen_account_id=citizen-asha \
  http://localhost:8088/supersede
```

## Phase 2 audit and monitoring

### Audit event object

Nullable credential fields are always present as JSON `null`. `details` is an object. Actor and department values are immutable snapshots from event time.

```json
{
  "id":42,
  "eventId":"opaque-id",
  "createdAt":"2026-08-18T10:30:45.123Z",
  "actor":{"id":"user-id","email":"official@example.gov","name":"Birth Official","role":"OFFICIAL"},
  "department":{"id":"birth","name":"Birth Registration Dept"},
  "action":"ISSUE",
  "outcome":"SUCCESS",
  "result":"VALID",
  "credential":{"docId":"BC-2026-1234","docHash":"0xabc...","citizen":"Asha Menon","transactionHash":"0xtx...","referenceHash":null},
  "requestId":"opaque-id",
  "httpStatus":201,
  "error":null,
  "details":{},
  "previousHash":"64 lowercase hex characters",
  "entryHash":"64 lowercase hex characters"
}
```

Actions: `ISSUE`, `VERIFY_FILE`, `VERIFY_ID`, `REVOKE`, `SUPERSEDE`, `USER_PROFILE_CREATE`, `USER_PROFILE_UPDATE`, `VERIFICATION_REQUEST_CREATED`, `CONSENT_NOTIFICATION`, `CONSENT_APPROVED`, `CONSENT_DENIED`, `VERIFICATION_REQUEST_EXPIRED`, `VERIFICATION_REQUEST_COMPLETED`, `CONSENT_TOKEN_REJECTED`.

Outcomes: `SUCCESS`, `FAILURE`, `DENIED`, `PARTIAL_FAILURE`.

An authenticated request rejected by application RBAC or department isolation is `DENIED`. A blockchain/RPC write failure after those checks is `FAILURE`, even though the existing credential-operation HTTP contract reports contract write errors with status `403`. A successful blockchain write followed by a failed SQLite document save is `PARTIAL_FAILURE` and includes the transaction hash when available.

### GET `/audit-events`

Purpose: paginated audit activity. Authentication: required. Controller sees system-wide history; Admin sees only its department; Official is forbidden.

Query parameters:

- `limit`: optional integer `1..100`, default `50`.
- `before`: optional positive event ID cursor.
- `action`: optional exact action enum.
- `outcome`: optional exact outcome enum.
- `department`: Controller filter; Admin may only provide its own department.
- `documentId`: optional exact document ID.
- `actorUserId`: optional exact actor ID.

Response `200`:

```json
{
  "events":[],
  "page":{"limit":50,"nextBefore":null,"hasMore":false}
}
```

When more results exist, `nextBefore` is the last returned event ID. Pass it as the next `before` value.

Errors: `400` invalid limit/cursor/action/outcome; `401`; `403` Official or Admin cross-department request; `500 {"error":"could not load audit events"}`.

Example:

```sh
curl -H 'Authorization: Bearer <token>' \
  'http://localhost:8088/audit-events?limit=25&action=ISSUE&outcome=SUCCESS'
```

### GET `/monitoring/overview`

Purpose: Controller system-wide activity, department statistics, recent activity, and audit-chain integrity. Authentication: required. Role: Controller only. Scope: system-wide. No parameters.

Response `200`:

```json
{
  "generatedAt":"2026-08-18T10:35:00Z",
  "auditIntegrity":{"valid":true,"eventCount":128,"firstInvalidEventId":null},
  "totals":{"total":128,"success":113,"failure":6,"denied":8,"partialFailure":1},
  "operations":{
    "issue":{"total":30,"success":28,"failure":1,"denied":0,"partialFailure":1},
    "verify":{"total":70,"success":66,"failure":1,"denied":3,"partialFailure":0},
    "revoke":{"total":12,"success":9,"failure":1,"denied":2,"partialFailure":0},
    "supersede":{"total":10,"success":8,"failure":1,"denied":1,"partialFailure":0}
  },
  "verificationResults":{"VALID":40,"SUPERSEDED":5,"REVOKED":4,"TAMPERED":8,"NOT_ISSUED":9,"UNKNOWN":0},
  "departments":[{
    "id":"birth","name":"Birth Registration Dept","active":true,
    "events":42,"success":38,"failure":2,"denied":2,"partialFailure":0,
    "issue":12,"verify":24,"revoke":3,"supersede":3,
    "lastActivityAt":"2026-08-18T10:30:45.123Z"
  }],
  "recentActivity":[]
}
```

`recentActivity` contains up to 10 complete audit event objects. Statistics cover only events recorded after Phase 2 audit deployment.

Errors: `401`; `403`; `500 {"error":"could not load monitoring overview"}`.

Example:

```sh
curl -H 'Authorization: Bearer <controller-token>' http://localhost:8088/monitoring/overview
```

## Phase 3 citizen consent

Supabase authenticates government users only. Citizens approve or deny through a random, expiring, request-specific email token. SQLite stores only the SHA-256 token hash. Raw tokens and consent URLs are never written to SQLite, audit details, or logs.

New credentials are stamped with `{PUBLIC_WEB_URL}/verify?docId={url-encoded-document-id}` before hashing and anchoring. Existing PDFs with bare-ID QR values remain valid and readable. The QR frontend creates a consent request and does not display a registry verdict before completion.

Verification requests expire 15 minutes after creation. States are `PENDING`, `APPROVED`, `DENIED`, `EXPIRED`, and `COMPLETED`. An identical repeated approve/deny is idempotent; conflicting decisions and repeated completion return `409`. Expiry is enforced when requests or tokens are read or changed.

### GET `/citizen-accounts`

Purpose: list active provisioned citizen accounts for the Issue form. Authentication: required. Roles: Admin or Official. Scope: government issuance UI. Email is masked.

Response `200`:

```json
[{"id":"citizen-asha","displayName":"Asha Menon","email":"a***@example.test"}]
```

Errors: `401`; `403`; `500 {"error":"could not load citizen accounts"}`.

Example:

```sh
curl -H 'Authorization: Bearer <token>' http://localhost:8088/citizen-accounts
```

Citizen accounts are provisioned through controlled local tooling, not a public API:

```sh
cd backend
go run ./cmd/citizen-seed -db credentials.db -id citizen-asha \
  -name 'Asha Menon' -email asha@example.test
```

`-phone` and `-supabase-user-id` are optional. `-link-doc-id` explicitly links existing stored credentials; no name/email matching occurs.

### Verification request object

Government request APIs return:

```json
{
  "id":"opaque-request-id",
  "documentId":"BC-2026-1234",
  "requesterUserId":"supabase-sub",
  "requesterEmail":"official@example.gov",
  "requesterName":"Birth Official",
  "requesterRole":"OFFICIAL",
  "departmentId":"birth",
  "departmentName":"Birth Registration Dept",
  "documentType":"birth_certificate",
  "state":"PENDING",
  "purpose":"Employment onboarding",
  "createdAt":"2026-08-18T10:30:45Z",
  "expiresAt":"2026-08-18T10:45:45Z",
  "decisionAt":null,
  "completedAt":null,
  "decisionChannel":null,
  "decisionReference":null,
  "completedResult":null,
  "version":1,
  "notificationStatus":"SUCCEEDED",
  "notificationDestination":"a***@example.test"
}
```

The citizen account ID, reference hash, and approval-token hash are not serialized. `completedResult` is the chain-backed verification object after completion.

### POST `/verification-requests`

Purpose: request one-time citizen consent. Authentication: required. Roles: Admin or Official. Scope: caller department. Controller is forbidden.

JSON body:

```json
{"documentId":"BC-2026-1234","purpose":"Employment onboarding"}
```

`purpose` is required and limited to 500 characters. The current chain record must belong to the caller department, and its stored document must have an active `citizen_account_id`.

Response `201`:

```json
{
  "id":"opaque-request-id",
  "state":"PENDING",
  "expiresAt":"2026-08-18T10:45:45Z",
  "notification":{"channel":"EMAIL","destination":"a***@example.test","status":"SUCCEEDED"}
}
```

Errors: `400` malformed/missing purpose or document ID; `401`; `403` Controller or cross-department credential; `404` unknown credential; `409` unlinked/inactive citizen account; `500` persistence failure; `502` blockchain or development-notification failure. A notification failure leaves the request recorded and audited as failed.

### GET `/verification-requests`

Purpose: list authorized requests. Authentication: required. Admin sees its department; Official sees only requests it created in its department. Controller is forbidden.

Query parameters: `state` optional state enum; `limit` integer `1..100`, default `50`; `offset` non-negative integer, default `0`.

Response `200`:

```json
{"requests":[],"page":{"limit":50,"offset":0,"hasMore":false}}
```

Errors: `400` invalid filters; `401`; `403`; `500` store failure.

### GET `/verification-requests/{id}`

Purpose: read request status. Authentication: required. Original Official may read its request; department Admin may read any request in its department. Controller and other departments are forbidden.

Response `200`: verification request object above. Tokens are never included.

Errors: `401`; `403`; `404`; `500`.

### POST `/verification-requests/{id}/complete`

Purpose: atomically consume an approval and perform chain-backed verification. Authentication: required. Original Official or a scoped department Admin. State must be `APPROVED`.

Request body: none.

The backend rereads the request's anchored credential hash from Solidity at completion. Approval is consent only; a later revocation or supersession is returned as the current `REVOKED` or `SUPERSEDED` verdict. Completion must occur before the request expiry timestamp, even when approval was granted earlier.

Response `200`:

```json
{"request":{"id":"opaque-request-id","state":"COMPLETED"},"verification":{"docId":"BC-2026-1234","status":"VALID","expectedHash":"0xabc...","message":"authentic and current"}}
```

The real `request` value is the complete request object.

Errors: `401`; `403`; `404`; `409` not approved, expired, denied, or already completed; `500` persistence/reference failure; `502` blockchain read failure.

### GET `/consent/{token}`

Purpose: public one-time email-link context. Authentication: none. The token itself authenticates access to this single request.

Response `200` contains only:

```json
{
  "requestId":"opaque-request-id",
  "state":"PENDING",
  "requester":{"name":"Birth Official","role":"OFFICIAL"},
  "department":{"id":"birth","name":"Birth Registration Dept"},
  "documentType":"birth_certificate",
  "purpose":"Employment onboarding",
  "expiresAt":"2026-08-18T10:45:45Z"
}
```

Errors: `404` invalid token; `409` already decided/used; `410` expired; `500` store failure.

### POST `/consent/{token}/approve`

Purpose: atomically transition `PENDING` to `APPROVED` and append its audit event. Authentication: one-time token. Body: none.

Response `200`:

```json
{"id":"opaque-request-id","state":"APPROVED","decisionAt":"2026-08-18T10:35:00Z"}
```

An identical repeated approval returns `200`; a denial followed by approval returns `409`.

Errors: `404` invalid token; `409` conflicting/terminal state; `410` expired; `500` atomic persistence failure.

### POST `/consent/{token}/deny`

Same contract as approve, transitioning to `DENIED`. An identical repeated denial is idempotent. A denied request cannot be completed.

### GET `/development/notifications/{id}`

Purpose: authenticated local-demo substitute for email delivery. Roles/scope match request access: original Official or department Admin. Controller is forbidden.

Response `200`:

```json
{"requestId":"opaque-request-id","consentUrl":"http://127.0.0.1:5173/consent/<raw-token>","developmentOnly":true}
```

This is the only API that exposes the raw consent URL. It is authenticated, scoped, held only in process memory, and unavailable after backend restart. It must be removed or disabled when a real notifier is configured. The URL/token is never persisted or audited.

Errors: `401`; `403`; `404` capture unavailable.

## Temporary diagnostic endpoint

### GET `/auth-test`

Purpose: temporary JWT diagnostic. Authentication: valid Supabase bearer token. Application profile and role are not loaded or checked. Do not use this response for authorization.

Response `200`:

```json
{"id":"supabase-user-id","email":"jwt-email@example.gov"}
```

Errors: `401` missing/malformed/invalid bearer token.

Example:

```sh
curl -H 'Authorization: Bearer <token>' http://localhost:8088/auth-test
```

## Contract-change rule

Existing request and success-response contracts are frozen for frontend integration. Additive or breaking changes must be implemented and documented here in the same change. Human roles and department assignments remain backend-owned SQLite state; Supabase remains the authentication authority; Solidity remains the credential and department-wallet authority.
